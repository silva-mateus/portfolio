services:
  portfolio:
    build: .
    container_name: portfolio-app
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge